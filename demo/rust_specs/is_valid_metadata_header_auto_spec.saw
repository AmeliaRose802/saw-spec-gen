// Auto-generated spec for: is_valid_metadata_header
// Constraints derived from type system and annotations

let is_valid_metadata_header_auto_spec : LLVMSetup () = do {

    // Parameter: header
    header_ptr <- llvm_alloc_readonly (llvm_array 32 (llvm_int 8));
    header_before <- llvm_fresh_var "header" (llvm_array 32 (llvm_int 8));
    llvm_points_to header_ptr (llvm_term header_before);

    llvm_execute_func [header_ptr];

    ret <- llvm_fresh_var "ret" (llvm_int 1);
    llvm_return (llvm_term ret);

    // Postconditions
    llvm_points_to header_ptr (llvm_term header_before);
};

// Usage: ov_is_valid_metadata_header <- llvm_unsafe_assume_spec m "is_valid_metadata_header" is_valid_metadata_header_auto_spec;
