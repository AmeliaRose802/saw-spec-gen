// Auto-generated spec for: validate_remote
// Constraints derived from type system and annotations

let validate_remote_auto_spec : LLVMSetup () = do {

    // Parameter: token
    token_ptr <- llvm_alloc_readonly (llvm_array 32 (llvm_int 8));
    token_before <- llvm_fresh_var "token" (llvm_array 32 (llvm_int 8));
    llvm_points_to token_ptr (llvm_term token_before);

    llvm_execute_func [token_ptr];

    ret <- llvm_fresh_var "ret" (llvm_alias "async_coroutine<validate_remote>");
    llvm_return (llvm_term ret);

    // Postconditions
    llvm_points_to token_ptr (llvm_term token_before);
};

// Usage: ov_validate_remote <- llvm_unsafe_assume_spec m "validate_remote" validate_remote_auto_spec;
