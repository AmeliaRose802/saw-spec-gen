// Auto-generated spec for: is_valid_claims_header
// Constraints derived from type system and annotations

let is_valid_claims_header_auto_spec : LLVMSetup () = do {

    // Parameter: claims
    claims_ptr <- llvm_alloc_readonly (llvm_array 16 (llvm_int 8));
    claims_before <- llvm_fresh_var "claims" (llvm_array 16 (llvm_int 8));
    llvm_points_to claims_ptr (llvm_term claims_before);

    llvm_execute_func [claims_ptr];

    ret <- llvm_fresh_var "ret" (llvm_int 1);
    llvm_return (llvm_term ret);

    // Postconditions
    llvm_points_to claims_ptr (llvm_term claims_before);
};

// Usage: ov_is_valid_claims_header <- llvm_unsafe_assume_spec m "is_valid_claims_header" is_valid_claims_header_auto_spec;
