# Soft-exit (status=not_attempted) when the target fn has no matching
# LLVM symbol. Auto-discovered from the sibling Cryptol spec (replaces
# the former --spec-only-on-missing CLI flag).
[functions.nonexistent_fn]
spec_only_on_missing = true
